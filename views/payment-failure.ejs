<%- include('partials/header', { title, q: '' }) %>

  <section class="result-page">
    <div class="container">
      <div class="result-card failure">
        <div class="result-icon">✕</div>
        <h1>Payment Not Completed</h1>
        <p>Your transaction was cancelled or failed. Your cart is safe — you can try again anytime.</p>
        <div class="result-actions">
          <a href="/checkout" class="btn btn-primary">Retry Checkout</a>
          <a href="/cart" class="btn btn-ghost">Back to Cart</a>
        </div>
      </div>
    </div>
  </section>

<%- include('partials/footer') %>